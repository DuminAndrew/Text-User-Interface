# 🚀 Как запустить проект

## Одна команда для всего!

### Windows
Откройте командную строку в папке проекта и запустите:
```cmd
build_and_test.bat
```

### Linux/Mac
Откройте терминал в папке проекта и запустите:
```bash
./build_and_test.sh
```

---

## Что произойдет автоматически:

1. ✅ Проверка зависимостей (Ninja, CMake)
2. ✅ Очистка build директории
3. ✅ Конфигурация CMake с Ninja
4. ✅ Сборка проекта (~30-60 сек)
5. ✅ Запуск всех 33 unit-тестов (~0.3 сек)
6. ✅ Создание демо лог-файла (если нужно)
7. ✅ **Запуск приложения с демонстрацией!**

---

## Результат:

```
========================================
Build and Tests SUCCESSFUL!
========================================

Executable: build\log_analyzer.exe
Tests: All 33 tests passed

[5/5] Running demo application...

Starting Log Analyzer with demo.log...
Press Q or ESC to quit the application
```

После этого откроется TUI приложение с демонстрационным логом!

---

## Управление в приложении:

| Клавиша | Действие |
|---------|----------|
| ↑ / ↓ | Навигация по строкам |
| PgUp / PgDn | Прокрутка страницами |
| Home / End | Переход к началу/концу |
| H | Вкл/Выкл подсветку синтаксиса |
| Q / Esc | Выход |

---

## Файлы проекта:

### 📁 Скрипты сборки:
- `build_and_test.bat` - **ГЛАВНЫЙ СКРИПТ ДЛЯ WINDOWS**
- `build_and_test.sh` - **ГЛАВНЫЙ СКРИПТ ДЛЯ LINUX/MAC**
- `create_demo_log.bat` - создание демо-лога (Windows)
- `create_demo_log.sh` - создание демо-лога (Linux/Mac)

### 📁 Исходный код:
- `src/main.cpp` - точка входа
- `src/log_reader.cpp/hpp` - чтение больших файлов через mmap
- `src/filter_engine.cpp/hpp` - regex фильтрация
- `src/syntax_highlighter.cpp/hpp` - подсветка SQL/JSON
- `src/tui_display.cpp/hpp` - TUI интерфейс

### 📁 Тесты:
- `tests/test_log_reader.cpp` - 8 тестов
- `tests/test_filter_engine.cpp` - 12 тестов
- `tests/test_syntax_highlighter.cpp` - 13 тестов

### 📁 Документация:
- `QUICK_START.md` - быстрый старт
- `BUILD_AND_TEST_GUIDE.md` - подробное руководство
- `README.md` - описание проекта

### 📁 Демо:
- `demo.log` - 47 строк демо-лога

---

## Требования:

### Windows:
- CMake 3.14+
- Ninja Build System
- Компилятор C++20 (MSVC / MinGW-GCC / Clang)

### Linux:
```bash
sudo apt-get install cmake ninja-build g++
```

### macOS:
```bash
brew install cmake ninja
```

---

## Только сборка (без запуска):

Если нужно только собрать без запуска приложения:

```bash
mkdir build
cd build
cmake -G Ninja -DCMAKE_BUILD_TYPE=Release ..
ninja
```

---

## Только тесты:

```bash
cd build
ctest --output-on-failure
```

Или напрямую:
```bash
./build/log_analyzer_tests
```

---

## Запуск с вашим лог-файлом:

```bash
./build/log_analyzer /path/to/your/logfile.log
```

Приложение поддерживает файлы размером до **50+ GB** благодаря memory-mapped файлам!

---

## Проблемы?

### "Ninja not found"
Установите Ninja: https://github.com/ninja-build/ninja/releases

### "CMake not found"
Установите CMake: https://cmake.org/download/

### Ошибки компиляции
Убедитесь что у вас C++20 компилятор:
- GCC 10+
- Clang 10+
- MSVC 2019+

---

## Вопросы?

См. подробную документацию:
- [BUILD_AND_TEST_GUIDE.md](BUILD_AND_TEST_GUIDE.md)
- [QUICK_START.md](QUICK_START.md)
- [README.md](README.md)
